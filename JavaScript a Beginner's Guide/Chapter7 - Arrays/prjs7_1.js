let computer_parts = ["Monitors", "Motherboards", "Chips", "Hard Drives", "DVD-ROMs"
, "Cases", "Power Supplies"];

// Display each computer part on its own line in the browser window (use a loop).
for (let i = 0; i < computer_parts.length; i++)
    document.write(computer_parts[i] + "<br>");
